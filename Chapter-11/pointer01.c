//#include <stdio.h>
//
//int main(void)
//{
//	int nData = 10;
//	printf("%s\n", "nData");
//
//	printf("%d\n", nData);
//
//	printf("%p\n", &nData);
//	printf("%#x\n", 0x10);
//	printf("%#x\n", &nData);
//
//	printf("%d\n", (long int) &nData);
//	return 0;
//	/*
//	이름이 nData인 부호가 있는 32비트 정수형 메모리의 실제 주소는 0x0019FED8이고, 그 안에 저장된 정보를 해석하면 10진 정수인 10이다.
//	이름이 nData인 부호가 있는 32비트 정수형 메모리의 실제 주소는 0x0019FED8이고, 그 안에 저장된 정보를 해석하면 10진 정수인 10이다.
//
//	이름이 nData인 부호가 있는 32비트 정수형 메모리의 실제 주소는 0x0019FED8이고, 그 안에 저장된 정보를 해석하면 10진 정수인 10이다.
//	이르밍 nData인 부호가 있는 32비트 정수형 메모리의 실제 주소는 0x0019FED8이고, 그 안에 저장된 정보를 해석하면 10진 정수인 10이다.
//	*/
//
//}